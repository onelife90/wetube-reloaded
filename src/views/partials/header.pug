header 
    a(href="/").header__logo
        i.fab.fa-youtube
    nav 
        ul 
            li 
                a(href="/search") 
                    i.fas.fa-search
            if loggedIn
                li 
                    a(href="/videos/upload") Upload Videos
                li 
                    a(href="/users/edit") Edit Profile
                li
                    a(href="/users/logout") Log Out
                li 
                    a(href=`/users/${loggedInUser._id}`)
                        if (!loggedInUser.avatarUrl)
                            span 😎 
                        else 
                            if user.avatarUrl.includes("https://")
                                img.header__avatar(src=user.avatarUrl, crossorigin)
                            else
                                img.header__avatar(src=`/${user.avatarUrl}`, crossorigin)
            else
                li 
                    a(href="/login") Login
                li 
                    a(href="/join").header__btn Join